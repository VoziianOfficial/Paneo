
document.documentElement.classList.add('js');
document.addEventListener('DOMContentLoaded',()=>{initPremiumMagneticButtons();initAnimatedCounters();initSmartMobileMenuGestures();injectAnimatedLines();});
function initPremiumMagneticButtons(){document.querySelectorAll('.btn').forEach(btn=>{btn.addEventListener('mousemove',e=>{const r=btn.getBoundingClientRect(),x=e.clientX-r.left-r.width/2,y=e.clientY-r.top-r.height/2;btn.style.transform=`translate(${x*.08}px, ${y*.12}px)`});btn.addEventListener('mouseleave',()=>btn.style.transform='')})}
function initAnimatedCounters(){const counters=document.querySelectorAll('[data-count-to]');if(!counters.length)return;const observer=new IntersectionObserver(entries=>{entries.forEach(entry=>{if(!entry.isIntersecting)return;const el=entry.target,target=Number(el.dataset.countTo||0),suffix=el.dataset.countSuffix||'',start=performance.now(),duration=1100;function frame(now){const p=Math.min((now-start)/duration,1),eased=1-Math.pow(1-p,3);el.textContent=Math.round(target*eased).toLocaleString('en-US')+suffix;if(p<1)requestAnimationFrame(frame)}requestAnimationFrame(frame);observer.unobserve(el)})},{threshold:.35});counters.forEach(c=>observer.observe(c))}
function initSmartMobileMenuGestures(){const menu=document.querySelector('[data-mobile-menu]'),toggle=document.querySelector('[data-menu-toggle]');if(!menu||!toggle)return;let startX=0;menu.addEventListener('touchstart',e=>startX=e.touches[0].clientX,{passive:true});menu.addEventListener('touchend',e=>{const endX=e.changedTouches[0].clientX;if(endX-startX>80&&menu.classList.contains('is-open')){menu.classList.remove('is-open');toggle.classList.remove('is-active');toggle.setAttribute('aria-expanded','false');document.body.style.overflow=''}},{passive:true})}
function injectAnimatedLines(){document.querySelectorAll('.metric-strip,.timeline-section,.service-examples,.about-proof,.contact-trust').forEach(section=>{if(section.querySelector('.lux-line'))return;const svg=document.createElementNS('http://www.w3.org/2000/svg','svg');svg.setAttribute('class','lux-line');svg.setAttribute('viewBox','0 0 1200 90');svg.setAttribute('preserveAspectRatio','none');svg.innerHTML='<path d="M0 54 C180 10 290 80 440 38 S720 22 850 58 1040 96 1200 34" />';section.style.position='relative';section.appendChild(svg)})}
